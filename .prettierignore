node_modules
.next
out
dist
build
coverage
public
prisma/migrations
*.lock
package-lock.json

# Vendored public-domain Scripture store (compact one-line JSON per book)
src/lib/content-shared/bible/dra/*.json

# Generated liturgical-day golden table (one row per line, ~37k rows)
intelligence/data/*.json

# USCCB daily-readings title fixture (one date per line)
tests/content/fixtures/*.json

# Xcode-owned asset catalogs (iPhone app) — Xcode rewrites these in its own
# style whenever the project is opened, so formatting them here only churns.
ios/**/*.xcassets
ios/build
ios/build-sim
ios/build-signed
